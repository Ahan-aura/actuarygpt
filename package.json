{
  "name": "actuarygpt-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && rm -rf ../dist ../build && cp -r dist ../dist && cp -r dist ../build"
  }
}
